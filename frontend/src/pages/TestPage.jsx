import React from 'react';
// import { useParams } from 'react-router-dom';

function TestPage() {
  // const {param} = useParams();
  
  return (
    <div>
      <h2>Hi, this is a test page to check if the authentication and route redirection works.</h2>
    </div>
  );
}

export default TestPage;
